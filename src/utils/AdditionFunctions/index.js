import { useSelector } from "react-redux";

/* ADDitions FUNCtions */ 

const O = {};
  
export default O;




// O.search2 = (todo, value) => {
//   return todo.map((e) => {
//     if (e.TodoTitle.match(new RegExp(`${value}`, "g"))) return e;
//   });
// };